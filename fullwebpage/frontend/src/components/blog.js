import React from 'react';

const blogs = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Blogs</h1>
    </div>
  );
};

export default blogs;