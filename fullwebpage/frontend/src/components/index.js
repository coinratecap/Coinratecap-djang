import React from 'react';

const coins = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Coins</h1>
    </div>
  );
};

export default coins;